#ifndef __FIND_H__
#define __FIND_H__
#include "main.h"
// extern float x, y;  //x y坐标
// extern double xielv;
// 定义PID结构体
// typedef struct {
//     float Kp; 
//     float Ki; // 积分系数
//     float Kd; // 微分系数
//     float setpoint; // 设定值
//     float integral; // 积分值
//     float last_error; // 上一次的误差
// } PID;
// void PID_Init(PID *pid, float Kp, float Ki, float Kd, float setpoint);
// float PID_Calculate(PID *pid, float current_value);
// int find_x(int x);
// int find_y(int y);
// int wai(double slope);
// void color(uint8_t color);
#endif
